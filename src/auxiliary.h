#ifndef AUXILIARY_H

#define AUXILIARY_H

#include <time.h>
#include <stdlib.h>


size_t at(size_t height, size_t row,
        size_t col, size_t nRow, size_t nCol);

#endif /* end of include guard: AUXILIARY_H */
